# Cymbol
A set of simple header only libraries and executables for trivial stuff

cymbol.h:
    header for packing/unpacking data

cymath.h:
    header for some basic math functionality

cympage.h:
    simple page allocator implementation

finspect.c:
    executable for inspecting file data

toc.c:
    executable that writes images in .c format (needs std_image.h)

pythum.py:
    simple library for simple quantum circuit building and simulation (ongoing)
    TODO: implement multi conditional and multi target gates